# C题问题4技术设计文档

> 本文档面向 Codex，用于实现 C 题问题4。问题2和问题3已经完成并通过交叉验证，因此问题4应最大限度复用现有代码，仅扩展价格数据维度并分别重新运行问题2、问题3逻辑。  
> 本文档不要求远端进行数值求解。  
> 输出文件为 `result4-2.xlsx` 和 `result4-3.xlsx`。

## 1. 实现目标

问题4需要在附件4给出的动态电价条件下重新计算问题2和问题3。

实现两个独立入口：

```text
question4_2
question4_3
```

其中：

```text
question4_2 = dynamic price + question2 model
question4_3 = dynamic price + question3 model
```

禁止为了第四问重新设计问题2或问题3已经验证通过的场景、SOC、紧急购电、光伏预测和滚动调整逻辑。

## 2. 允许读取的文件

问题4-2允许读取：

```text
data/附件2.xlsx
data/附件4.xlsx
target/附件5/result4-2.xlsx
问题2既有代码
```

如问题2公共模块需要，也可以读取附件1中的时间标签或公共配置，但**不得再使用附件1固定电价作为问题4的价格输入**。

问题4-3允许读取：

```text
data/附件2.xlsx
data/附件3.xlsx
data/附件4.xlsx
target/附件5/result4-3.xlsx
问题3既有代码
```

问题4完成时不得修改：

```text
result1.xlsx
result2.xlsx
result3.xlsx
```

## 3. 附件4实际结构

已核对附件4：

```text
Sheet1
A1:EO366
```

结构为：

```text
A列     日期
B:EO    144个10分钟电价
```

共：

```text
365天 × 144点
```

日期范围：

```text
2025-01-01 ~ 2025-12-31
```

价格单位：

```text
元/kWh
```

其日期和时间粒度与附件2完全一致。

## 4. 动态价格数据类型

新增：

```python
@dataclass
class AnnualPriceData:
    dates: list[date]
    price_yuan_per_kwh: np.ndarray
```

要求：

```python
price_yuan_per_kwh.shape == (365, 144)
```

建议建立日期到行索引：

```python
date_to_index: dict[date, int]
```

并提供：

```python
def get_daily_price(
    annual_price: AnnualPriceData,
    target_date: date,
) -> np.ndarray:
    ...
```

返回：

```text
shape = (144,)
```

## 5. 价格数据验证

加载附件4以后必须检查：

```text
365个日期
每个日期144个价格
无NaN
无Inf
全部为数值
日期严格连续
```

同时与附件2日期进行对齐：

```python
assert price_dates == actual_data.dates
```

不能按行号默认为同一天而不验证日期。

## 6. 最重要的价格索引规则

对于目标日期 `d`：

```python
daily_price = price_data[d]
```

必须保证该日所有费用项都使用同一个 144 点动态价格向量。

不要使用：

```text
附件1固定电价
全年平均电价
前一天电价
历史31天平均电价
```

替代附件4目标日价格。

## 7. 基础建模口径

题目提供了目标日完整动态电价数据，但没有提供电价预测数据。

因此基础版本规定：

```text
附件4目标日144点价格
作为题目给定的当日结算价格曲线
```

不额外实现价格预测模型。

不要在问题4中擅自新增：

```text
ARIMA价格预测
LSTM价格预测
价格随机场景
实时价格MPC
```

这些可以作为论文扩展，但不是主实现。

## 8. 代码复用原则

第四问推荐新增非常薄的适配层，而不是复制问题2、问题3全部代码。

理想接口应逐步统一为：

```python
solve_question2_day(
    target_date,
    daily_price,
    ...
)

solve_question3_day(
    target_date,
    daily_price,
    ...
)
```

如果当前问题2、问题3内部直接引用固定 `price` 全局变量，应将价格改为显式函数参数。

改动后必须保证原问题2和问题3仍然能够传入附件1固定价格正常运行。

## 9. 强制回归原则

第四问改造优化器以后，必须做回归测试。

将附件1固定价格向量重复成每天相同的价格矩阵：

```python
constant_price_matrix = np.tile(
    attachment1_price[None, :],
    (365, 1),
)
```

使用这一矩阵运行问题4-2逻辑时，应能够复现问题2。

使用这一矩阵运行问题4-3逻辑时，应能够复现问题3。

也就是说：

$$
\text{Q4-2 engine + fixed price}
\approx
\text{Q2 result}
$$

以及

$$
\text{Q4-3 engine + fixed price}
\approx
\text{Q3 result}.
$$

这是问题4最重要的工程验收测试之一。

## 10. 问题4-2求解流程

正式日期继续为：

```text
2025-02-01 ~ 2025-12-31
```

每天：

```text
读取目标日动态价格
        ↓
读取此前31天净负荷场景
        ↓
调用问题2随机LP
        ↓
得到0:00计划购电和储能计划
        ↓
使用目标日真实负荷/PV进行事后结算
        ↓
按目标日动态价格计算紧急购电费
        ↓
保存结果
```

历史场景窗口、概率和 no-lookahead 规则全部复用问题2。

## 11. 问题4-2目标函数

原问题2：

$$
\min
\sum_tq_tG_t
+
5\sum_s\pi_s\sum_tq_tE_{s,t}.
$$

问题4-2改为：

$$
\boxed{
\min
\sum_tq_{d,t}G_t
+
5\sum_s\pi_s\sum_tq_{d,t}E_{s,t}
+
\varepsilon\sum_t(C_t+D_t)
}.
$$

代码中：

```python
objective_grid = daily_price

objective_emergency = (
    5.0
    * scenario_probability[:, None]
    * daily_price[None, :]
)
```

其余 LP 矩阵结构不变。

## 12. 问题4-2实际费用

```python
planned_cost = np.dot(
    daily_price,
    grid_kwh,
)

emergency_cost = np.dot(
    5.0 * daily_price,
    real_emergency_kwh,
)

total_cost = (
    planned_cost
    + emergency_cost
)
```

全天实际外网购电量：

```python
total_grid_purchase = (
    grid_kwh.sum()
    + real_emergency_kwh.sum()
)
```

## 13. result4-2.xlsx结构

已核对模板，与 `result2.xlsx` 同构，有三个工作表：

```text
计划购电量
充放电量
紧急购电量
```

“计划购电量”：

```text
A1:EQ335
A      日期
B:EO   144点计划购电量
EP     全天购电量
EQ     全天购电费
```

写入规则应与问题2最终已经验证的 exporter 完全一致，只更换目标文件。

如果问题2当前规定：

```text
EP = 计划购电量 + 紧急购电量
EQ = 计划购电费 + 紧急购电费
```

则问题4-2保持相同语义。

“充放电量”和“紧急购电量”也直接复用问题2输出规则。

## 14. 问题4-3求解流程

每天：

```text
读取目标日动态价格
        ↓
0:00构造问题3场景
        ↓
使用动态价格求初始计划
        ↓
执行到下一更新时间
        ↓
获取新光伏预测
        ↓
使用同一目标日动态价格的剩余切片
重新优化剩余时段
        ↓
逐次计算调整交易费用
        ↓
实际不足按5倍动态价格紧急购电
        ↓
保存最终执行策略
```

## 15. 问题4-3的更新时间固定为 0+6+12

第三问的消融实验已经完成，并且最终得到的最优更新时间策略为：

```python
Q3_FINAL_UPDATE_HOURS = (0, 6, 12)
```

即：

```text
0:00   制定全天计划
6:00   第一次滚动调整
12:00  第二次滚动调整
18:00  不再进行调整
```

因此问题4-3必须直接继承这一正式策略：

```python
Q4_3_UPDATE_HOURS = (0, 6, 12)
```

推荐直接从问题3配置中导入：

```python
Q4_3_UPDATE_HOURS = Q3_FINAL_UPDATE_HOURS
assert Q4_3_UPDATE_HOURS == (0, 6, 12)
```

问题4-3主模型**不得再使用 18:00 预测更新**。

原因是第三问已经通过消融实验验证：

```text
0+6+12
```

的全年总购电费用低于：

```text
0+6+12+18
```

因此 `0+6+12` 已经是第三问正式选定策略。第四问的任务是研究“固定电价变为动态电价”以后该策略的表现，而不是重新搜索预测更新时间集合。

为了保证问题3与问题4-3之间具有严格可比性，第四问只改变：

```text
固定价格 q[t]
        ↓
动态价格 q[d,t]
```

而保持：

```text
更新时间 = (0, 6, 12)
场景构造方法不变
预测误差模型不变
SOC约束不变
调整结算规则不变
紧急购电机制不变
```

18:00 预测仅可在论文中作为第三问消融实验的历史对照结果引用，不进入问题4-3正式求解流程。

## 16. 问题4-3初始计划目标函数

0:00：

$$
\boxed{
\min
\sum_tq_{d,t}G_t
+
5\sum_s\pi_s\sum_tq_{d,t}E_{s,t}
+
\varepsilon\sum_t(C_t+D_t)
}.
$$

实现时只需把问题3原来的：

```python
price
```

替换成：

```python
daily_price
```

## 17. 问题4-3调整阶段价格切片

某次更新：

```python
start_slot = ISSUE_SLOT[issue_hour]
```

对应未来价格：

```python
price_remaining = daily_price[start_slot:]
```

必须与：

```text
old_grid
new_grid
charge
discharge
scenario horizon
```

长度完全一致。

例如：

```text
6:00   108点
12:00  72点
18:00  36点
```

## 18. 问题4-3调整费用

完全复用问题3的经济解释，只替换价格向量。

```python
adjustment_cashflow = (
    np.dot(
        1.5 * price_remaining,
        up_adjust,
    )
    -
    np.dot(
        0.5 * price_remaining,
        down_adjust,
    )
)
```

数学形式：

$$
C^{adj}
=
\sum_t
\left(
1.5q_{d,t}U_t
-
0.5q_{d,t}V_t
\right).
$$

禁止在问题4重新将减少购电解释为额外罚款。

## 19. 问题4-3紧急购电费用

最终执行策略完成以后：

```python
emergency_cost = np.dot(
    5.0 * daily_price,
    real_emergency,
)
```

注意：

```text
每个10分钟紧急购电量
必须乘该时间段自己的动态价格
```

禁止使用当天平均价格或调整时刻单一价格。

## 20. 问题4-3总费用

```python
plan_cost = np.dot(
    daily_price,
    initial_grid,
)

adjustment_cost = sum(
    revision.adjustment_cashflow_yuan
    for revision in revisions
)

emergency_cost = np.dot(
    5.0 * daily_price,
    real_emergency,
)

total_cost = (
    plan_cost
    + adjustment_cost
    + emergency_cost
)
```

与问题3保持完全同口径。

## 21. result4-3.xlsx结构

已核对模板，与 `result3.xlsx` 同构，共四个工作表：

```text
计划购电量
调整购电量
充放电量
紧急购电量
```

“计划购电量”：

```text
A1:EQ335
```

填写 0:00 初始购电计划。

“调整购电量”：

```text
A1:EQ335
```

填写问题3最终定义的“实际执行时生效的最新合同购电量”，不是调整差值。

“充放电量”使用最终执行充放电。

“紧急购电量”复用问题3连续区间合并逻辑。

EP/EQ 的含义必须与正式 `result3.xlsx` 保持一致，不能在第四问改变。

## 22. 推荐代码目录

建议：

```text
src
├── question1
├── question2
├── question3
└── question4
    ├── __init__.py
    ├── config.py
    ├── price_loader.py
    ├── question4_2.py
    ├── question4_3.py
    ├── comparison.py
    ├── validator.py
    └── main.py
```

第四问目录应尽量薄。

核心优化器继续复用：

```text
question2.optimizer
question3.initial_optimizer
question3.adjustment_optimizer
question3.rolling_controller
```

## 23. 不建议复制问题2和问题3代码

禁止创建：

```text
question4_optimizer_copy.py
question4_3_controller_copy.py
```

这类整段复制版本。

正确方式是将价格变成参数后复用原实现。

否则问题2、问题3和问题4容易产生三套几乎相同但细节逐渐漂移的代码。

## 24. 公共价格接口建议

建议最终统一：

```python
def solve_day(
    ...,
    price_kwh: np.ndarray,
):
    assert price_kwh.shape == (horizon,)
```

问题2传：

```python
attachment1_price
```

问题4-2传：

```python
attachment4_price[target_date]
```

问题3滚动阶段传：

```python
attachment1_price[start_slot:]
```

问题4-3滚动阶段传：

```python
attachment4_price[target_date][start_slot:]
```

这样同一个 optimizer 可以直接服务固定电价和动态电价。

## 25. 日期对齐验证

对任意目标日期：

```python
idx_actual = actual_data.date_to_index[target_date]
idx_price = price_data.date_to_index[target_date]
```

必须验证：

```python
actual_data.dates[idx_actual]
==
price_data.dates[idx_price]
==
target_date
```

问题4-3还应验证附件3对应预测日期一致。

## 26. 时间列对齐验证

附件2和附件4均为：

```text
B:EO = 144个10分钟点
```

建议比较两者原始时间表头。

如果表头经过 Excel 类型转换后语义一致，则按列位置映射。

不得：

```text
排序时间字符串
手工把0:00+1挪到前面
```

## 27. 数值验证

每一天继续执行问题2/问题3已有的：

```text
场景电量平衡
SOC动态
SOC上下界
充放电上限
日末SOC
实际供需平衡
紧急购电非负
```

问题4新增价格相关检查：

```python
assert np.all(np.isfinite(daily_price))
```

如题目数据不存在负电价，则可以额外验证：

```python
assert np.all(daily_price >= 0)
```

但不要擅自修改合法输入数据。

## 28. 费用验证

问题4-2：

```python
assert abs(
    total_cost
    - (
        np.dot(daily_price, grid)
        + np.dot(
            5.0 * daily_price,
            real_emergency,
        )
    )
) <= COST_TOL
```

问题4-3：

```python
assert abs(
    total_cost
    - (
        np.dot(daily_price, initial_grid)
        + sum(revision_cashflows)
        + np.dot(
            5.0 * daily_price,
            real_emergency,
        )
    )
) <= COST_TOL
```

## 29. 固定电价回归测试

这是第四问必须写的测试。

### Q4-2回归

构造：

```python
fake_dynamic_price = np.tile(
    attachment1_price,
    (365, 1),
)
```

运行问题4-2引擎。

至少在若干代表日期上验证：

```text
grid
charge
discharge
emergency
total_cost
```

与问题2一致或在 LP 等价最优容差内一致。

### Q4-3回归

同样使用重复固定价格。

验证问题4-3与问题3的：

```text
initial_grid
executed_grid
charge/discharge
emergency
total_cost
```

一致。

如果回归失败，应优先排查第四问改造，而不是直接接受新结果。

## 30. 结果比较模块

新增：

```text
comparison.py
```

用于问题论文分析，而不是优化。

至少输出：

```text
Q2固定电价全年总费用
Q4-2动态电价全年总费用
费用绝对变化
费用相对变化

Q3固定电价全年总费用
Q4-3动态电价全年总费用
费用绝对变化
费用相对变化
```

还建议输出：

```text
计划购电量
紧急购电量
紧急购电费
总充电量
总放电量
发生紧急购电天数
最大单日费用
平均单日电价
```

## 31. 价格波动统计模块

建议计算：

```python
daily_mean_price
daily_min_price
daily_max_price
daily_price_range
daily_price_std
```

其中：

$$
\text{price range}
=
\max_tq_{d,t}
-
\min_tq_{d,t}.
$$

可进一步研究：

```text
price range vs battery throughput
price range vs total cost
price range vs emergency cost
```

这些分析不影响官方 Excel 输出。

## 32. 四个指定日期

仍建议对前三问已经使用的四个典型日期输出第四问摘要：

```text
2025-03-20
2025-06-21
2025-09-23
2025-12-21
```

问题4-2输出：

```text
动态价格
计划购电
紧急购电
全天总购电量
全天总购电费
```

问题4-3输出：

```text
0:00初始计划
最终执行购电
调整净费用
紧急购电
全天总购电量
全天总购电费
```

方便与问题2、问题3横向比较。

## 33. 输出文件安全

正式运行前：

```text
不要直接覆盖官方模板
```

建议：

```text
result4-2.tmp.xlsx
result4-3.tmp.xlsx
```

全部 334 天求解和验证通过后再替换正式文件。

任一日期失败：

```text
立即停止
不要生成半成品正式结果
```

## 34. 进度输出

问题4-2：

```text
[001/334] 2025-02-01 solved
price_mean = ...
plan_kWh = ...
emergency_kWh = ...
total_cost = ...
```

问题4-3：

```text
[001/334] 2025-02-01
0:00 solved
6:00 adjusted
12:00 adjusted
...
total_cost = ...
```

具体更新时间集合以问题3最终配置为准。

## 35. 推荐单元测试

至少加入：

```text
test_attachment4_shape
test_attachment4_date_range
test_attachment4_no_nan
test_attachment4_time_alignment
test_attachment2_attachment4_date_alignment
test_daily_price_lookup
test_q42_dynamic_price_objective
test_q42_emergency_dynamic_price
test_q43_initial_dynamic_price
test_q43_adjustment_dynamic_price
test_q43_emergency_dynamic_price
test_q43_price_horizon_slice
test_q43_inherits_q3_update_hours
test_q42_fixed_price_regression
test_q43_fixed_price_regression
test_result42_template_structure
test_result43_template_structure
test_result42_cost_identity
test_result43_cost_identity
```

## 36. 禁止事项

问题4实现时禁止：

1. 用附件1固定电价替代附件4；
2. 对附件4价格取全年平均以后再优化；
3. 擅自建立新的负载场景方法；
4. 擅自改变问题2的 31 天窗口；
5. 擅自改变问题3的光伏预测误差模型；
6. 擅自改变问题3最终采用的更新时间集合；
7. 改变问题3增加/减少购电的结算口径；
8. 用平均电价计算紧急购电费用；
9. 用单一更新时刻电价代替未来每个 slot 的动态价格；
10. 修改问题1、问题2、问题3正式结果；
11. 复制整套问题2/问题3代码形成独立漂移版本；
12. 引入遗传算法；
13. 引入价格预测模型作为主实现；
14. 任一日期求解失败后仍导出正式结果。

## 37. Definition of Done

问题4只有满足以下条件才算完成：

```text
附件4成功读取为365×144动态价格矩阵
附件2和附件4日期严格对齐
问题4-2完全复用问题2模型
问题4-3完全复用问题3模型
所有价格项使用目标日期动态价格
紧急购电使用5倍目标时段动态价格
问题4-3调整费用使用动态价格
问题4-3更新时间固定为 `(0, 6, 12)`，与第三问最终最优策略一致
334天问题4-2全部成功
334天问题4-3全部成功
所有物理约束通过
所有费用恒等式通过
固定电价回归测试能够复现问题2和问题3
result4-2.xlsx正确生成
result4-3.xlsx正确生成
论文比较摘要成功输出
不修改前三问正式结果
```

## 38. 给 Codex 的最终任务指令

阅读 `docs/需求.pdf`、问题2和问题3的既有技术文档以及 `docs/问题4_技术设计.md`，只实现 C 题问题4。问题2和问题3已经通过验证，因此不得重新设计其负荷/PV不确定性、储能、紧急购电和滚动预测模型。

读取 `data/附件4.xlsx`，建立 2025-01-01 至 2025-12-31 的 `365×144` 动态电价矩阵，并严格与附件2日期和时间列对齐。问题4基础版本不新增电价预测模型，直接将附件4目标日期的 144 点价格作为题目给定的当日动态价格条件。

实现问题4-2时，完整复用问题2的 31 天等权历史净负荷场景和两阶段随机 LP，只将固定价格 `q[t]` 替换为目标日动态价格 `q[d,t]`。计划购电费用使用 `q[d,t]`，实际不足仍按 `5*q[d,t]` 计算紧急购电费用。输出到 `target/附件5/result4-2.xlsx`，工作表语义必须与正式 `result2.xlsx` 完全一致。

实现问题4-3时，完整复用问题3最终采用的预测更新时间集合、光伏小时预测插值、历史预测误差场景、SOC滚动连续性和逐次调整结算逻辑，只把全部价格系数替换成目标日动态价格。0:00 初始计划使用 `q[d,t]`；每次增购按 `1.5*q[d,t]`，减购按 `0.5*q[d,t]` 退款；实际紧急购电按 `5*q[d,t]`。输出到 `target/附件5/result4-3.xlsx`，工作表语义必须与正式 `result3.xlsx` 完全一致。

第四问不得擅自重新选择问题3的更新时间。第三问已经通过消融实验确定正式最优更新时间为 `(0,6,12)`，因此问题4-3必须固定使用 `(0,6,12)`，18:00 不再参与正式滚动调整。第四问应保证与第三问的比较中，主要变化只有电价。

必须新增固定电价回归测试：将附件1固定日电价重复到所有日期后，问题4-2引擎应能够复现问题2，问题4-3引擎应能够复现问题3。若回归不通过，先修复第四问价格接口和费用计算，不能直接接受结果。

所有 334 个正式日期求解并验证成功后，再生成正式 `result4-2.xlsx` 和 `result4-3.xlsx`。任何日期或滚动阶段求解失败时立即停止，不输出半成品正式结果。问题4完成后停止。
